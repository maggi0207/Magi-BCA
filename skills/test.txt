You are inspecting the HCA HCP Credentialing repository.

ROLE:
Act as a senior engineer. Discovery only. Do not modify any files. Do not implement ADD NPP.

TASK:
Find the existing "PAF Action & Facilities" / Practitioner Action radio list so tickets 132255–132259 can reuse it. Cursor will use your RETURN block to write the 132256 implementation prompt. Incomplete paths = not ready to code.

BUSINESS REQUIREMENT (do not implement now):
Feature 132255: add a Practitioner Action radio labeled exactly "ADD NPP to Facility".
132256: visible only if user has MSP role; last in the list; if role cannot be retrieved, hide and log.
132257: show only for Net New OR existing practitioner inactive with the entity; hide if active with the entity or otherwise ineligible.
132259: keep single-select radios including the new option; client + server must reject multiple selections and log.
132258: on submit of ADD NPP to Facility, reuse existing New Practitioner vs Existing Practitioner PAF task determination. No new NPP determination engine.

SCOPE:
Read-only search. No code changes. No new files. No TaskType.Npp wiring. No new PafType.

SEARCH FOR (literal strings):
"PAF Action & Facilities"
"Practitioner Action"
"Add Practitioner to Facility"
"ADD NPP to Facility"
"Add Privilege MSS-18"
"Off-Cycle RRFC"
"Off-Cycle PSV"
"Request Processing Change"
"Practitioner Personal Info"
PafTaskSelection
TaskType
GetAvailableTaskListAsync
TaskListParameters
TaskAllowedListMode
AddPractitionerToFacility
IsMspOnlyUser
IsMspUser
IsInRole

INVESTIGATION STARTING POINTS (confirm or say Not found):
- Client/Pages/Paf/Components/PafTaskSelection.razor
- Client/Pages/Paf/Components/PafTaskSelection.razor.cs
- Shared/Data/Model/Paf/TaskType.cs
- Hca.Credentialing.Paf.Api/Controllers/TaskController.cs
- Hca.Credentialing.Paf.Api/Services/TaskDeterminationService.cs (or wherever ITaskDeterminationService is implemented)
- How facilities are listed beside the action radios (Filter Facilities / Select All Facilities)

DO NOT:
- Invent file paths.
- Implement the new radio.
- Treat 136883 dashboard/NPP search URL auth as this dialog’s MSP check unless you open both and prove they share a helper.
- Collapse 132257 eligibility into TaskDeterminationService unless the same method already hides actions by active/inactive entity.
- Propose a new dialog or a new determination engine.

BEFORE RESPONDING:
1. Search the repo with the strings above.
2. Open matching files and read markup, visibility, submit, and determination calls.
3. Quote real paths, types, and methods.

RETURN exactly this structure:

## Discovery result — 132255 PAF Action

### A. Dialog / page
File (.razor and .cs):
Class/Component:
Dialog title in UI (exact):
When it is shown (caller page + method):
Tests:
Current behavior:
Is this "PAF Action & Facilities"? (yes/no + proof)

### B. Practitioner Action radio list (132256 / 132259)
File where radios are rendered:
How options are sourced (enum / API / hardcoded):
Exact current option labels in order (last existing option name):
Radio group mechanism (same name / component):
Client selection handler:
What is posted on Next/submit (single value vs list + property names):
Server endpoint that receives the selected action:
Existing server check that only one action is selected (file/method or Not found):
Where a validation error would be logged (file/method or Not found):
Tests:
Current behavior:
Smallest extension point to add "ADD NPP to Facility" as last radio:
Files that must not change:

### C. MSP role visibility (132256)
How this dialog currently shows/hides actions by role:
Exact helper / IsInRole / policy (quote):
What happens if role lookup fails today:
Existing log on role failure (or Not found):
Same helper as MSP Dashboard Begin NPP PAF (136883)? (yes/no + both file paths):
Tests:
Reusable for hide-unless-MSP? (yes/no + why)

### D. Eligibility (132257)
How net-new vs existing is known on this dialog:
How practitioner–entity active vs inactive is stored and read:
When eligibility runs (dialog open vs after facility checked):
Do any existing actions already hide for active-at-entity? (which + file):
Tests:
Reusable for SHOW net-new OR existing-inactive; HIDE active / other? (yes/no + why)
Unknowns:

### E. Task determination after submit (132258)
File/class/method for New Practitioner PAF determination:
File/class/method for Existing Practitioner PAF determination:
How selected action chooses which rules run:
TaskAllowedListMode or equivalent (quote):
Failure today: UI error, block submit, log, partial task set? (quote):
Tests:
Reusable without a new NPP engine? (yes/no + why)
Does TaskType.Npp already exist? (file + is it in this radio list today?)

### F. Tests that must keep passing
- list real test files for this dialog / Add Practitioner to Facility / determination

### G. Recommended first code change (for later 132256 only)
File to change:
Why this is the smallest safe change:
Files that must not change:

### H. Unknowns
- anything not found
