You are implementing a change in the HCA HCP Credentialing repository.

ROLE:
Senior engineer. One session. Inspect only the files listed as already discovered, then implement. Do not start a second chat.

TASK:
1) Pass existing PractitionerSearchPage._isNppEntry into Add New and Verify forms as IsNppRequest.
2) 131794/131795: when IsNppRequest, Add New required = First, Last, Degree, Provider Category, Individual NPI. Optional = Email, Cell, DOB, SSN, Gender, Suffix.
3) 132247 family: when IsNppRequest, existing email read-only (PropertyDescription); blank email editable and not required; ExtendedEmailAddress still runs only when a value is entered.

Treat inspect RETURN as fact unless you re-read and disprove:
- _isNppEntry is private on PractitionerSearchPage (from NavigationManager.Uri). ForceNpiSearchMode goes only to PractitionerSearchForm. Table does not get NPP.
- IsNpiOptional/_isNoNpiUser = Recruitment || PSG. Do NOT use that for NPP. Do NOT turn on ShowNoNpiOption for NPP.
- Add New: AddNewPractitionerForm + NewPractitionerModel. ShouldRequireField required unless IsRecruitmentRole. ShouldRequireNpi required only if IsPsgRole (MSP NPI optional today).
- Category = hardcoded APP/DHP/Physician/Other. Leave it.
- Duplicate NPI already in ConfirmationWizardModal.SaveNewPractitionerAsync / PractitionerDuplicateCheckHelper. Do not change helper.
- Verify: VerifyExistingPractitionerForm + VerifyPractitionerModel. Email always InputHca; ShouldRequireField unless IsRecruitmentRole. Identity card already uses PropertyDescription (reuse for read-only email).
- ExtendedEmailAddress Required defaults false. Do not edit ExtendedEmailAddress.cs or Npi.cs.
- No tests for these forms. Do not create a test project.

COMMENTS:
Do not add ticket-ID comments, essays, or region banners. No // 131794, // NPP isolation, // 132247. Match existing Parameter style only (if the file already has [Parameter] with no XML, add none). One-word names are enough.

STOP AND REPORT (do not invent):
- If _isNppEntry is gone or not URI-based and you would need a new feature flag or auth policy.
- If you must edit PractitionerSearchForm, PageUri, Paf.Api, or 136883 authorize/redirect to plumb the flag.

THEN IMPLEMENT (smallest reuse):

Plumbing
- PractitionerSearchPage.razor: pass _isNppEntry into PractitionerSearchTable (new [Parameter] bool, same name style as existing table params).
- PractitionerSearchTable: hold it; pass into ConfirmationWizardModal.
- ConfirmationWizardModal: [Parameter] bool IsNppRequest (or IsNppEntry — pick one name and use it end-to-end). Pass to AddNewPractitionerForm and VerifyExistingPractitionerForm.
- Do not change [Authorize], NPP URL redirect, or ForceNpiSearchMode behavior.

131794 / 131795
- Set NewPractitionerModel.IsNppRequest from the parameter in OnInitializedAsync (same place as IsPsgRole / IsRecruitmentRole).
- ShouldRequireField: if IsNppRequest, return Success for the optional demographics (same members ShouldRequireField already gates: DOB, Gender, SSN, Email, Phone). First/Last/Degree/Category stay [Required] as today.
- ShouldRequireNpi: if IsNppRequest, NPI is required (blank = Required field). Do not set ShowNoNpiOption true for NPP. Do not show No NPI radio/Reason on NPP.
- Begin PAF (IsNppRequest false): zero behavior change.

132247 / 132250 / 132252 / 132248 / 132253
- Verify: set model IsNppRequest the same way.
- If IsNppRequest && !string.IsNullOrWhiteSpace(Practitioner.EmailAddress): render Email with existing PropertyDescription (Label Email, Value the address). No InputHca. User cannot edit.
- Else (NPP and blank, or not NPP): keep current InputHca.
- VerifyPractitionerModel.ShouldRequireField: if IsNppRequest, Email is not required (blank Success). Non-NPP unchanged.
- Do not change ExtendedEmailAddress. Invalid typed email still fails format.
- Do not change the identity PropertyDescription block except reusing it for Email when read-only.
- Begin PAF Verify unchanged.

DO NOT CHANGE:
- PractitionerSearchForm.razor / .cs
- PageUri.cs
- PractitionerSearchPage authorize / NPP redirect (136883)
- ExtendedEmailAddress.cs, Npi.cs, PractitionerDuplicateCheckHelper.cs
- Paf.Api, PafTaskSelection, TaskDeterminationService
- Add New input markup structure (no new fields, no V5 "starts with 1 or 20")
- Recruitment/PSG No-NPI UI

IMPLEMENTATION REQUIREMENTS:
1. Reuse existing Parameter + ShouldRequire* pattern.
2. Begin PAF and other roles unchanged.
3. No duplicate forms.
4. No invented flags, policies, or NPI N/A.
5. Almost no new comments.

TESTS:
- Do not create a test project.
- dotnet build the Client (or the project that hosts these pages).
- If an existing test project already references these types, add nothing unless a test breaks — then fix.

VALIDATION: build, inspect diff (comments should be near-zero).

RETURN:
1. Files changed
2. Parameter name used end-to-end
3. Quote ShouldRequireField and ShouldRequireNpi NPP branches
4. How Verify chooses PropertyDescription vs InputHca
5. Confirm ShowNoNpiOption is false for NPP
6. Build result
7. Remaining gaps
8. Assumptions
