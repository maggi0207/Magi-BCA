`NavigateToStartPaf()` is unchanged (still exactly `NavigationManager.NavigateTo(PageUri.PractitionerSearchUri)`); the new `NavigateToStartNppPaf()` is a separate, additional method. 6 files changed, all directly required for 136878/136883; the other 4 files in status are pre-existing unrelated local changes.

## Final Report

**136878 status: DONE**
**136883 status: DONE** (via reuse of existing authorization conventions — no new policy invented)

### Exact files changed
1. `Client/Pages/Shared/PageUri.cs` — added `PractitionerNppSearchUri = "/Practitioner/Search/Npp"`
2. `Client/Pages/PractitionerSearch/PractitionerSearchPage.razor` — added a second `@page "/Practitioner/Search/Npp"` directive; passed `ForceNpiSearchMode="@_isNppEntry"` to `PractitionerSearchForm`
3. `Client/Pages/PractitionerSearch/PractitionerSearchPage.razor.cs` — injected `NavigationManager`; added `_isNppEntry` (compares the matched relative path to the new URI)
4. `Client/Pages/PractitionerSearch/Components/PractitionerSearchForm.razor.cs` — added `[Parameter] public bool ForceNpiSearchMode`; changed the MSP-only branch condition from `if (!_isRecruitmentUser && !_isPsgUser)` to `if (!_isRecruitmentUser && !_isPsgUser && !ForceNpiSearchMode)`
5. `Client/Pages/Queues/Components/PafQueueSearchCriteria.razor` — added `OnClick="@NavigateToStartNppPaf"` to the Begin NPP PAF button
6. `Client/Pages/Queues/Components/PafQueueSearchCriteria.razor.cs` — added `NavigateToStartNppPaf()`, a new method mirroring `NavigateToStartPaf()` exactly, navigating to `PageUri.PractitionerNppSearchUri`

### New route/URL identity
`/Practitioner/Search/Npp` — a second, static `@page` directive on the **same** `PractitionerSearchPage` component (no new page, no new search workflow). This is the "distinct URL-addressable identity" per your requirement.

### How NpiSearch is selected
`PractitionerSearchPage._isNppEntry` compares `NavigationManager.ToBaseRelativePath(NavigationManager.Uri)` against `PractitionerNppSearchUri` and passes that as `ForceNpiSearchMode` into `PractitionerSearchForm`. Inside `PractitionerSearchForm.OnInitializedAsync()`, the only change is that the pre-existing MSP-only `SwitchToExceptionSearch()` call is now skipped when `ForceNpiSearchMode` is true — no search-mode logic was rewritten; `SwitchToNpiSearch`/`SwitchToExceptionSearch`/`SearchMode` are untouched. An MSP user hitting `/Practitioner/Search/Npp` simply keeps the component's default (`SearchMode.NpiSearch`), which is the same NPI-required screen Recruitment/PSG already see.

### How authorization is enforced
The existing `@attribute [Authorize(Roles = "Msp, Psg, Recruiter, RecruiterManager")]` on `PractitionerSearchPage` was **not modified** and applies to both `@page` routes on the component (Blazor authorization is per-component, not per-route). This means the new `/Practitioner/Search/Npp` URL automatically inherits the exact same role gate as today's `/Practitioner/Search` — any role outside Msp/Psg/Recruiter/RecruiterManager (Cpc, Css, Admin, Practitioner, Delegate, etc.) is blocked from the NPP URL by direct navigation, identical to how they're already blocked from the existing search page. No new policy/claim/framework was introduced, per constraint #9.

### Confirmation existing Begin PAF remains unchanged
- `NavigateToStartPaf()` — unchanged (verified via grep, single match, same body)
- `PageUri.PractitionerSearchUri` — unchanged value
- MSP hitting `/Practitioner/Search` still has `_isNppEntry == false` → `ForceNpiSearchMode == false` → `SwitchToExceptionSearch()` still runs exactly as before
- Recruitment ("Begin Request") and PSG: `_isRecruitmentUser`/`_isPsgUser` branch is untouched; the new condition only adds an `&& !ForceNpiSearchMode` guard inside the branch that already excluded them
- `PafQueueSearchCriteria`'s "Begin PAF" button and Recruitment "Begin Request" button markup: unchanged

### Build result
`dotnet build` on `Hca.Credentialing.Client` — succeeded, no warnings or errors.

### Manual/automated validation performed
No Blazor client test project exists (unchanged from prior findings) — none created, per instructions. Validation performed:
- Build success
- `git status` confirms only the 6 files above touched (plus pre-existing unrelated local changes: `settings.json`, `tasks.json`, `.sln`, `launchSettings.json`, none touched by this task)
- Grep-confirmed `NavigateToStartPaf()` body is byte-identical to before
- Traced the full conditional chain in `PractitionerSearchForm.OnInitializedAsync()` to confirm Recruitment/PSG paths are structurally untouched

### Final git diff summary
6 files, all additive/minimal:
- 1 new constant (`PageUri.cs`)
- 1 new `@page` directive + 1 new bound parameter (`PractitionerSearchPage.razor`)
- 1 injected service + 1 computed property (`PractitionerSearchPage.razor.cs`)
- 1 new parameter + 1 added `&&` condition, no logic rewritten (`PractitionerSearchForm.razor.cs`)
- 1 `OnClick` attribute added to the already-existing Begin NPP PAF button (`PafQueueSearchCriteria.razor`)
- 1 new method mirroring the existing one (`PafQueueSearchCriteria.razor.cs`)

No blockers encountered. 136871 (all captured children: 136873, 136876, 136878, 136882 regression, 136883) is now **COMPLETE** based on repository evidence and captured ticket acceptance criteria.
