136883 only. Do not redo 136873/136876/136878. Do not change Begin PAF.

BUG (UI evidence): PSG Dashboard /Psg/Dashboard shows Begin NPP PAF next to Begin PAF. That fails 136883. MSP-only may start Begin NPP PAF. PSG/Recruitment existing search must stay.

CAUSE: Begin NPP PAF sits in the CanBeginPaf && !IsRecruitmentUser else branch. IsMspUser() is Msp OR Psg (PrincipalExtensions). Same MspDashboardPage serves /Msp/Dashboard and /Psg/Dashboard.

DO NOT:
- Invent a new policy/claim (not 131728)
- Change NavigateToStartPaf() or PageUri.PractitionerSearchUri
- Change PractitionerSearchPage [Authorize(Roles = "Msp, Psg, Recruiter, RecruiterManager")] — that would break Begin PAF / PSG / Recruitment on /Practitioner/Search
- Remove or retarget the NPP URI /Practitioner/Search/Npp
- Hide Begin PAF from PSG

1. UI: show Begin NPP PAF only when current user IsInRole("Msp"). Do not use IsMspUser(). Reuse PrincipalExtensions / AuthenticationState already on the queue components. Inspect before adding a new parameter.

2. Direct URL: if _isNppEntry and user is not IsInRole("Msp"), deny using the app’s existing unauthorized pattern (AuthorizeView/NotAuthorized, existing redirect — find one, do not invent). PSG/Recruitment must still use /Practitioner/Search.

3. Build Client. Diff only the files required for this gate. Report exact role check and deny behavior.

STOP.
