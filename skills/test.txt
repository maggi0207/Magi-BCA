You are implementing a change in the HCA HCP Credentialing repository.

ROLE:
Act as a senior engineer familiar with the existing HCP Blazor Client patterns.

TASK:
Implement Azure DevOps user story 136873 only — NPP: Add Begin NPP PAF action to MSP Dashboard.

This is a markup-only UI change. Stop when 136873 is complete. Do not start 136876, 136878, 136882, or 136883.

BUSINESS REQUIREMENT:
As an MSP user, I want a dedicated Begin NPP PAF option so that I can initiate requests for Non-Privileged Practitioners.

Acceptance criteria:
- Given the user is on the MSP Dashboard, when the page loads, both Begin PAF and Begin NPP PAF are displayed.
- Begin NPP PAF appears immediately adjacent to Begin PAF.
- The new button is keyboard accessible, screen-reader compatible, and styled consistently with existing application controls.

This story does not define click routing (136878), tooltip (136876), or authorization (136883).

REPOSITORY EVIDENCE:
File: Client/Pages/Queues/Components/PafQueueSearchCriteria.razor
Existing Extra-template rendering:

@if (CanBeginPaf)
{
    @if (IsRecruitmentUser)
    {
        <Button HtmlType="button" Type="@ButtonType.Primary" OnClick="@NavigateToStartPaf">Begin Request</Button>
    }
    else
    {
        <Button HtmlType="button" Type="@ButtonType.Primary" OnClick="@NavigateToStartPaf">Begin PAF</Button>
    }
}

CanBeginPaf is a [Parameter] supplied by PafQueueSearch as CanBeginPaf="@CanBeginPAF()".
CanBeginPAF() returns true for IsMspUser() or IsRecruitmentUser().
IsMspUser() = roles Msp or Psg.
IsRecruitmentUser() = roles Recruiter or RecruiterManager.
NavigateToStartPaf() calls NavigationManager.NavigateTo(PageUri.PractitionerSearchUri) with no extra context.

PafQueueSearchCriteria is reached via:
MspDashboardPage | CpcDashboardPage | CssDashboardPage | RecruitmentDashboardPage | Admin/HomePage
→ SharedPafQueue
→ PafQueueSearch
→ PafQueueSearchCriteria

EXISTING PATTERN TO REUSE:
The existing Ant Design Button in PafQueueSearchCriteria.razor Extra template:
HtmlType="button", Type="@ButtonType.Primary".
Reuse that exact control. Put the new button in the existing non-Recruitment else branch, immediately adjacent to Begin PAF.

SCOPE:
Change this file only:
- Client/Pages/Queues/Components/PafQueueSearchCriteria.razor

If and only if the markup cannot compile without it, a click stub in:
- Client/Pages/Queues/Components/PafQueueSearchCriteria.razor.cs
Prefer no .cs change. Prefer no OnClick on the new button (136878 owns navigation). Do not add a new handler that calls NavigateToStartPaf.

DO NOT CHANGE:
- CanBeginPAF()
- CanBeginPaf
- IsRecruitmentUser behavior
- Begin PAF button
- Begin Request button
- NavigateToStartPaf()
- PageUri.PractitionerSearchUri
- PafQueueSearch.razor / PafQueueSearch.razor.cs
- SharedPafQueue.razor / SharedPafQueue.razor.cs
- Any dashboard page
- PageUri.cs
- Practitioner search
- authorization / policies / roles
- tooltip
- APIs / services / models

Do not implement:
- NPP navigation
- Enforce NPI Search
- SearchMode
- query parameters
- NPP PAF creation

IMPLEMENTATION REQUIREMENTS:
1. Read PafQueueSearchCriteria.razor first. Confirm the CanBeginPaf / IsRecruitmentUser Extra-template block before editing.
2. Make the smallest possible markup-only change.
3. Do not refactor, rename, restyle, or reformat unrelated markup.
4. Do not modify unrelated files.
5. Do not invent business rules.
6. The new button must NOT appear in the Recruitment branch.

BEFORE CODING:
1. Inspect the existing Begin PAF / Begin Request markup.
2. Confirm the extension point is the non-Recruitment else branch.
3. State the planned one-line/adjacent-button change briefly.
4. Then implement.

IMPLEMENTATION:
In the existing non-Recruitment branch where Begin PAF is rendered, add a second button labeled exactly:

Begin NPP PAF

Required:
- immediately adjacent to Begin PAF
- same Button component
- same HtmlType="button"
- same Type="@ButtonType.Primary"
- same accessibility/keyboard/screen-reader pattern as Begin PAF
- visible only when CanBeginPaf is true AND IsRecruitmentUser is false

Target structure:

@if (CanBeginPaf)
{
    @if (IsRecruitmentUser)
    {
        Begin Request
    }
    else
    {
        Begin PAF
        Begin NPP PAF
    }
}

Do not wire NPP navigation. Ticket 136878 owns click/navigation. Ticket 136876 owns tooltip. Ticket 136883 owns security.

TESTS:
No Blazor client test project covers this component. Do not create a new test project.

Manual verification after the change:
- MSP Dashboard: Begin PAF and Begin NPP PAF both visible, adjacent
- Begin PAF unchanged
- Recruitment: Begin Request only; Begin NPP PAF not shown
- Keyboard focus can reach the new button
- Accessible name is Begin NPP PAF

VALIDATION:
1. Build the affected Client project (Hca.Credentialing.Client / the project that compiles PafQueueSearchCriteria.razor).
2. Review git diff. It should contain only the intended markup (and .cs only if a compile stub was unavoidable).
3. Confirm Begin Request and Begin PAF markup/handlers are unchanged.

RETURN:
1. Files changed
2. Implementation summary
3. Git diff summary
4. Build result
5. Warnings/errors
6. Remaining gaps (click/navigation still owned by 136878)
7. Stop. Do not continue to other tickets.
