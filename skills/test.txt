Read-only inspection complete. No files modified.

## Report — 53658 / 127104 Inspection

### 1. Exception Search — `PractitionerSearchForm.razor` + `.razor.cs`

**A — VERIFIED — ALREADY SATISFIED.**
- **Exception fields (MSP, non-flexible):** NPI (optional), SSN, First Name, Last Name only — no Reason/DOB/Email (those render only `if (IsFlexiblePafUser())`). Unchanged by the NPP route.
- **Minimum criteria / what blocks submit:** `IsExceptionButtonEnabled()` (`PractitionerSearchForm.razor.cs`) — for non-flexible users: `HasAnyNpi() || HasAnySsn() || (FirstName && LastName)`. Same method used regardless of entry route (NPP or Begin PAF); not modified by `ForceNpiSearchMode`.
- **`SwitchToExceptionSearch()` body:** unconditionally sets `_currentSearchMode = SearchMode.ExceptionSearch`, clears NPI/validation state, calls `OnResetCallback`. Not aware of `ForceNpiSearchMode` — untouched.
- **"No NPI available? Search without NPI" link:** rendered whenever `_currentSearchMode == SearchMode.NpiSearch` (razor condition has no `IsFlexiblePafUser()` guard on the link itself) — confirmed it **still renders and works** for an MSP user who lands in NpiSearch via `/Practitioner/Search/Npp`.
- **`ForceNpiSearchMode` scope:** read **once**, only inside `OnInitializedAsync()` (`if (!_isRecruitmentUser && !_isPsgUser && !ForceNpiSearchMode) SwitchToExceptionSearch();`). It is **not** a persistent lock — it does not block a later manual `SwitchToExceptionSearch()` call (e.g., via the "No NPI available?" link). It only affects the initial mode selected at load.

**Noted nuance (not a required fix, flagging for awareness only):** For an MSP user who reaches NpiSearch via the NPP route, then clicks "No NPI available?" (→ ExceptionSearch), then clicks "Cancel" — `SwitchToNpiSearch()`'s `else` branch (non-flexible users) calls `JsRuntime.InvokeVoidAsync("history.back")`, not a mode toggle back to NpiSearch. This is pre-existing, unchanged code (identical for today's Begin PAF flow), simply reachable via a path that didn't previously exist for MSP. No AC in 53658/127104 addresses this; not classified as a gap against those tickets.

### 2. Exception Audit

**B — VERIFIED GAP — CODE CHANGE REQUIRED** (existing reusable helper found).

- Grep of `Client/Pages/PractitionerSearch/**` for `QueueAuditLogEntry`, `ICredentialingAuditLogger`, `AuditAction` → **zero matches**. No client-side audit call exists in the search form/table.
- Traced the actual search execution server-side: `Hca.Credentialing.Cactus.Api/Controllers/PractitionerController.cs` → `SearchExistingPractitionersAsync` → `Hca.Credentialing.Cactus.Api/Data/Repositories/Practitioner/PractitionerRepository.cs` → `SearchPractitionersAsync(searchParameters, userId)`.
- Inside that exact method, for `searchParameters.SearchType == PractitionerSearchType.AffiliationVerification`, an audit call **already exists**:
  ```csharp
  await _serviceBusService.QueueAuditLogEntry(
      _auditLogger.GetAffVerifyPractitionerSearchAuditLog(
          searchParams.PractitionerId, searchParams.FirstName, searchParams.LastName,
          searchParams.Npi, searchParams.Ssn, searchParams.DateOfBirth));
  ```
- For `searchParameters.SearchType == PractitionerSearchType.Recruitment` (this is the exact `SearchType` the exception-search UI sets — see `PractitionerSearchForm.OnFinish()`: `searchParams.SearchType = PractitionerSearchType.Recruitment;`), **no audit call exists** in that branch.
- **Conclusion:** No audit event is currently created for exception practitioner searches. The exact existing, reusable mechanism is `Shared/Helpers/CredentialingAuditLogger.cs` (`ICredentialingAuditLogger`) + `IServiceBusService.QueueAuditLogEntry(AuditLog)`, already proven in this same method/file for a sibling search type. `GetAffVerifyPractitionerSearchAuditLog(practitionerId, firstName, lastName, npi, ssnLast4, dateOfBirth)` has a parameter shape that already matches the exception-search fields (minus Reason). No new `AuditAction`/schema is proposed here — that decision is left to implementation, not this inspection.

### 3. NPI Empty Results — `PractitionerSearchTable.razor` + `.razor.cs`

**A — VERIFIED — ALREADY SATISFIED.**
- Zero-result state: `HasNoResults()` → `!_tableLoading && _hasSearched && (_dataSource == null || !_dataSource.Any())`; card title becomes "No Results Found" and a message renders.
- "Add New Practitioner" button visibility: `ShouldShowAddButton()` → if `UseFormInputForAddButton` (true only for Recruitment/PSG) return `FormHasValidInput`; else return `_hasSearched`. **This method does not itself check `HasNoResults()`** — the button shows whenever a search has run (MSP) or the form has valid input (Recruitment/PSG), independent of whether results were returned. This is pre-existing behavior, unaffected by `ForceNpiSearchMode`.
- `UseFormInputForAddButton` is computed once in `PractitionerSearchPage.razor.cs` as `_currentUser.IsRecruitmentUser() || _currentUser.IsPsgUser()` — **not aware of `_isNppEntry`/`ForceNpiSearchMode` at all**. An MSP user on `/Practitioner/Search/Npp` therefore gets `UseFormInputForAddButton == false`, identical to MSP on `/Practitioner/Search` today — same `_hasSearched`-gated behavior, table component fully reused, untouched.
- Method that opens the modal: `ShowNewPractitionerPafModal()` (sets `_targetPractitioner = null; _showConfirmationModal = true;`) — same method, same component, reused unconditionally for the NPP route.

### 4. Add New Practitioner reuse

**A — VERIFIED — ALREADY SATISFIED.**
- `ConfirmationWizardModal` (`Client/Pages/PractitionerSearch/Components/ConfirmationWizard/ConfirmationWizardModal.razor.cs`) and `AddNewPractitionerForm` are rendered by the same `PractitionerSearchTable` regardless of route; no NPP-specific branching exists in either file (confirmed no `ForceNpiSearchMode`/`_isNppEntry` reference in `ConfirmationWizardModal.razor.cs` or `AddNewPractitionerForm.razor.cs`).
- `OnNewPractitionerModalComplete()` → `StartPafFrom(e.Practitioner, e.UpdatedEmailAddress)` (`PractitionerSearchTable.razor.cs`) — identical code path for New and Existing statuses, unconditional on route.
- No changes proposed to `AddNewPractitionerForm` or its required fields (131794 out of scope, untouched).

### 5. NPI Validation

**A — VERIFIED — ALREADY SATISFIED.**
- `ValidationRoutines.DoNpiValidation` called from `OnNpiFieldChanged()` — same method used in both `SearchMode.NpiSearch` (`_npiInputMask`) and non-flexible exception mode's NPI field (`_npiInputMaskNonRecruitment`).
- `NpiAttribute` (`[Npi]` on `SearchModel.PractitionerNpi`) and `NpiAttribute.Mask` sentinel — unchanged, referenced identically regardless of route.
- No `ForceNpiSearchMode`-conditional logic exists anywhere in the validation path.

---

**53658/127104 needs: audit hook only.**
