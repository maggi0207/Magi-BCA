4 files changed for this feature, exactly as intended; the rest are pre-existing unrelated local changes.

## RETURN

**1. Files changed**
- `Shared/Clients/Paf/TaskListParameters.cs`
- `Hca.Credentialing.Paf.Api/Controllers/TaskController.cs`
- `Hca.Credentialing.Paf.Api/Services/TaskDeterminationService.cs`
- `Hca.Credentialing.Paf.Api/Domain/Strategy/PafProcessingStrategyContext.cs`

**2. Classes/methods changed**
- `TaskListParameters` — added `IsMspUserMode` (bool) and `UserId` (string), both documented as server-computed-only.
- `TaskController.GetAvailableTaskListAsync` — resolves `IsMspUserMode`/`UserId` from `_httpContextAccessor.HttpContext.User` inside a try/catch; fails closed (`IsMspUserMode = false`) and logs via existing `_logger.LogWarning` on any failure.
- `TaskController.GetAvailableFacilityListAsync` — added `TaskType.Npp` to the allowed-task whitelist.
- `TaskDeterminationService.GetAvailableTaskListAsync` — appends the `Npp` `TaskDto` last in the NewPractitioner branch (MSP-only) and last in the ExistingPractitioner branch (MSP-only **and** eligible).
- `TaskDeterminationService.GetAvailableFacilityListAsync` — the existing `AddPractitionerToFacility` branch condition now also matches `TaskType.Npp` (`||`), reusing the identical facility filter.
- `PafProcessingStrategyContext.CreateAddTaskStrategy` — added `TaskType.Npp => new AddTaskAddPractitionerToFacilityStrategy(this)`.

**3. Quote — `CreateAddTaskStrategy` and Npp save mapping**
```csharp
return task switch
{
    TaskType.AddPractitionerToFacility => new AddTaskAddPractitionerToFacilityStrategy(this),
    // ADD NPP to Facility (132258) reuses the existing Add-Practitioner-to-Facility add-task workflow
    // as-is, so New vs Existing Practitioner mode already on the PAF continues to apply unchanged.
    TaskType.Npp => new AddTaskAddPractitionerToFacilityStrategy(this),
    TaskType.AddPrivilegesMss18 => new AddTaskAddPrivilegesMss18Strategy(this),
    ...
    _ => throw new ArgumentException($"Task \"{task}\" does not have an add strategy")
};
```
No new strategy class was created — `TaskType.Npp` maps to the exact same `AddTaskAddPractitionerToFacilityStrategy` instance construction as `AddPractitionerToFacility`, so the existing New-vs-Existing-practitioner initialization logic (`AddPractitionerToFacilitiesTask`, `SpecialtyTask`, PSG/`PerfTask` handling) applies unchanged.

**4. How MSP is evaluated (client/server)**
Server-only. `PafTaskSelection.razor.cs` is unchanged and never sets `IsMspUserMode` — it defaults to `false` client-side. `TaskController.GetAvailableTaskListAsync` overwrites it unconditionally from `_httpContextAccessor.HttpContext.User.IsInRole(nameof(HcoApplicationRole.Msp))` (the identical `IsInRole(Msp)` idiom already used for PSG/Recruitment in this same dialog and for 136883). A client-supplied value is never trusted. On any exception (null user, etc.) it fails closed to `false` and logs `_logger.LogWarning(ex, "Unable to determine Msp role for {Action}; ADD NPP to Facility will be hidden.", ...)`.

**5. Exact `TaskDto.Name`**
`"ADD NPP to Facility"` — a literal string, not `TaskType.Npp.GetDescription()` (which remains `"NPP PAF"` and was not changed).

**6. Eligibility query for 132257 + load-time interpretation**
Reused verbatim: `TaskDeterminationService`'s existing private `GetUserFacilitiesNotAddedToOrInactiveAtAsync(userId, practitionerKey)` (the same method `GetAvailableFacilityListAsync` already calls for `AddPractitionerToFacility`). For the ExistingPractitioner branch, called as `GetUserFacilitiesNotAddedToOrInactiveAtAsync(listParameters.UserId, practitioner.Key)` using the `practitioner` object already fetched in that branch. If the returned list is non-empty, Npp is shown; if empty, it's hidden (treated as active/ineligible everywhere). **Interpretation documented per your instruction:** since no facility is selected yet at Practitioner-Action-radio load time, "existing practitioner inactive with the entity" is evaluated as "has at least one facility where the practitioner is not-added-to-or-inactive" — the same interpretation already embodied by this exact query for the existing `AddPractitionerToFacility` action. NewPractitioner mode is always eligible (no facility context exists yet, mirrors `AddPractitionerToFacility`'s NewPractitioner branch).

**7. Facility list handling for Npp**
`GetAvailableFacilityListAsync`'s branch condition changed from `listParameters.Task == TaskType.AddPractitionerToFacility` to `listParameters.Task == TaskType.AddPractitionerToFacility || listParameters.Task == TaskType.Npp` — identical `GetUserFacilitiesAsync`/`GetUserFacilitiesNotAddedToOrInactiveAtAsync` filtering is reused for both. Also added `TaskType.Npp` to `TaskController.GetAvailableFacilityListAsync`'s allowed-task whitelist (previously would have 400'd).

**8. 132259 — extra server check or "already scalar"**
**Already scalar — no additional check added.** `PafTaskSelection.razor`'s `RadioGroup`/`foreach` markup was **not touched**; `TaskType.Npp` simply becomes another entry in the same data-driven, single-bound-value radio group. `TaskSelectionEvent.PafType` (`TaskType?`) and `PafController.AddTaskToPafAsync([FromQuery] TaskType task, ...)` were **not changed** — both are structurally single-valued types that cannot carry multiple selections. Per your instruction, since a realistic multi-select path is structurally impossible, no dummy validator was invented.

**9. Tests / build results**
- No `Hca.Credentialing.Paf.Api.Tests` project exists — confirmed (not created, per instructions).
- `dotnet build` on `Hca.Credentialing.Paf.Api`: **succeeded**, no warnings/errors.
- `dotnet build` on `Hca.Credentialing.Client`: **succeeded**, no warnings/errors (no client files were changed).
- `CrudEntityCredentialingOperationTests.GetMsoDueDate_ReturnsNull_ForAddPractitionerToFacilityWithTemporaryCredentialingTypeRtk`: **passed** (1/1), unaffected.
- One incidental environment issue encountered and resolved: a stale, previously-running `Hca.Credentialing.Paf.Api` process (PID 34148) was locking its own build output DLLs, causing transient MSB3026 copy-retry build failures unrelated to this code change; it was a leftover local dev process, stopped, and the build then succeeded cleanly.

**10. Remaining gaps (especially 132275)**
- **Task-card lifecycle (132275, explicitly out of scope):** `PafFormComponent.HandleTaskSelectionEvent`'s existing-PAF branch and `PafProcessingStrategyContext.CreateUpdateTaskStrategy` only special-case `TaskType.AddPractitionerToFacility` (and a few others) for task-status refresh / update-strategy resolution. If a PAF with `TaskSelected == TaskType.Npp` is later re-opened for editing, `CreateUpdateTaskStrategy`'s `default` case will throw `ArgumentException("Task \"Npp\" does not have an update strategy")`. Not fixed here — genuinely belongs to task-card/edit-flow work (132275), not the "Add" step this ticket covers.
- **Downstream PAF submission / CACTUS sync (out of scope, different project):** `Hca.Credentialing.Paf.Processor`'s `PafExtensions.GetCredentialingTypeRtk()` has no `TaskType.Npp` case and would throw `ArgumentOutOfRangeException` if a PAF with `TaskSelected == TaskType.Npp` is fully submitted. Confirmed via grep that `GetCredentialingTypeRtk()` is **never invoked** on the `AddTaskToPafAsync` path in `Hca.Credentialing.Paf.Api` (only in `Paf.Processor`, post-submission) — so this is not a STOP condition for 132255–132259 as scoped, but is a known follow-on blocker for whenever ADD NPP submission/CACTUS-sync work (132258's later phase / 132600) begins.
- VA/PA, PSV, Delegate/FSQ suppression, CVI, auto-acceptance: not touched, per explicit scope boundary.

**11. Assumptions**
- MSP-only visibility intentionally uses the raw `IsInRole(nameof(HcoApplicationRole.Msp))` idiom (not `IsMspUser()`, which also matches Psg) — consistent with your instruction and with 136883's precedent, on the assumption that MSP and PSG roles are not simultaneously assigned to the same user in practice (the same assumption 136883 already relies on).
- "Existing practitioner inactive with the entity" is interpreted at Practitioner-Action-radio load time as "has at least one not-added-or-inactive facility," per your explicit permission to use that interpretation given no facility is selected yet.
- `PractitionerSummary.Active` was **not** used anywhere in this implementation (per your explicit instruction not to guess its semantics).
