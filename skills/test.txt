Implement ONLY the verified audit gap for:

53658 — NPP PAF Search
127104 — Add NPP search – Add New Practitioner

IMPORTANT:
The NPP search workflow itself is ALREADY IMPLEMENTED through 136871.

DO NOT rebuild or modify the NPP search workflow.

The current verified state is:

MSP Dashboard
→ Begin NPP PAF
→ /Practitioner/Search/Npp
→ existing PractitionerSearchPage
→ existing PractitionerSearchForm
→ existing NpiSearch

The following have already been verified and MUST remain unchanged:

- NPP route
- PractitionerSearchPage
- ForceNpiSearchMode
- SearchMode.NpiSearch
- NPI validation
- SearchExistingPractitionersAsync
- PractitionerSearchTable
- Add New Practitioner workflow
- ConfirmationWizard
- StartPafFrom
- existing ExceptionSearch fields
- SwitchToExceptionSearch
- Begin PAF
- /Practitioner/Search
- MSP Begin PAF ExceptionSearch behavior
- PSG behavior
- Recruitment behavior
- authorization
- 131785
- 131794
- PafType / TaskType / downstream NPP workflow

==================================================
VERIFIED GAP
==================================================

The exception practitioner-search path does NOT currently create an audit event.

The flow is:

PractitionerSearchForm.OnFinish()
    ↓
searchParams.SearchType = PractitionerSearchType.Recruitment
    ↓
SearchExistingPractitionersAsync(...)
    ↓
PractitionerRepository.SearchPractitionersAsync(searchParameters, userId)

In PractitionerRepository.SearchPractitionersAsync():

- PractitionerSearchType.AffiliationVerification already creates an audit entry using:
  ICredentialingAuditLogger
  +
  IServiceBusService.QueueAuditLogEntry(...)

- PractitionerSearchType.Recruitment currently has NO equivalent audit call.

The existing audit infrastructure is already available.

==================================================
IMPLEMENTATION REQUIREMENT
==================================================

Add audit logging for the existing exception practitioner-search path ONLY.

Before editing:

1. Inspect CredentialingAuditLogger.cs / ICredentialingAuditLogger.
2. Inspect AuditLog / AuditAction and existing practitioner-search audit events.
3. Determine whether an existing audit method/event already semantically represents a practitioner search/exception search.
4. Search the repository for other practitioner-search audit implementations.
5. Inspect PractitionerSearchForm.OnFinish() for SearchType on NpiSearch vs ExceptionSearch.
6. Reuse an existing semantically appropriate audit method/event if one exists.

DO NOT blindly reuse GetAffVerifyPractitionerSearchAuditLog() merely because its parameter signature fits. It is specifically named for Affiliation Verification.

If NpiSearch also uses PractitionerSearchType.Recruitment, do NOT audit the whole Recruitment branch.
Use an existing discriminator for exception search only. If none exists, STOP.

If there is an existing generic practitioner-search audit event appropriate for exception search, reuse it.

If no appropriate existing audit event exists:

STOP and report the evidence.

DO NOT invent a new AuditAction, audit schema, or event type without sufficient existing repository evidence.

==================================================
SCOPE
==================================================

The audit should execute when the existing exception practitioner search actually executes.

Do not audit:

- opening the NPP search page
- opening the search form
- typing into the NPI field
- validation failure
- clicking the No-NPI link
- Add New Practitioner
- normal NPI search unless existing requirements explicitly require it

The verified gap is specifically the exception practitioner search execution.

Preserve all existing search behavior and return values.

==================================================
IMPLEMENTATION RULES
==================================================

- Minimal additive change only.
- Prefer the existing repository audit pattern.
- Do not refactor unrelated code.
- Do not change method signatures unless absolutely required.
- Do not create a new service.
- Do not create a new audit framework.
- Do not modify UI files unless repository evidence proves it is required.
- Do not modify the NPP routing implemented in 136871.
- Do not modify NPI validation.
- Do not modify exception search criteria.
- If you add the hook, fix only compile/using errors caused by THIS change.

==================================================
VALIDATION
==================================================

After implementation:

1. Build the affected project(s).
2. Verify no warnings/errors caused by the change.
3. Inspect git diff.
4. Confirm only files directly required for the audit change were modified.
5. Confirm Begin PAF behavior is unchanged.
6. Confirm NPP NPI search behavior is unchanged.
7. Confirm the audit is executed only on exception search execution.

If there is no existing semantically appropriate audit event and adding one would require inventing a new event/schema:

DO NOT IMPLEMENT THAT PART.

Instead report:

- what was found
- why existing events are not appropriate
- exact files involved
- what product/architecture decision is required.

==================================================
FINAL REPORT
==================================================

Return:

1. Files changed
2. Exact audit event/helper reused
3. Where the audit call was added
4. Why that event is semantically appropriate
5. NpiSearch vs ExceptionSearch SearchType (and any discriminator used)
6. Build result
7. Diff summary
8. Confirmation that NPP search and Begin PAF behavior were untouched
9. Any remaining blocker or decision required
   OR STOP evidence if no appropriate existing event exists
