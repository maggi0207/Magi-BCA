8 files changed, exactly matching this feature's scope (the other listed changes are pre-existing unrelated local artifacts). `PractitionerSearchForm`, `PageUri.cs`, `[Authorize]`/136883 redirect, `ExtendedEmailAddress.cs`, `Npi.cs`, `PractitionerDuplicateCheckHelper.cs`, Paf.Api, `PafTaskSelection`, `TaskDeterminationService` — none touched.

## RETURN

**1. Files changed (8)**
- `Client/Pages/PractitionerSearch/PractitionerSearchPage.razor`
- `Client/Pages/PractitionerSearch/Components/PractitionerSearchTable.razor` / `.razor.cs`
- `Client/Pages/PractitionerSearch/Components/ConfirmationWizard/ConfirmationWizardModal.razor` / `.razor.cs`
- `Client/Pages/PractitionerSearch/Components/ConfirmationWizard/AddNewPractitionerForm.razor.cs`
- `Client/Pages/PractitionerSearch/Components/ConfirmationWizard/VerifyExistingPractitionerForm.razor` / `.razor.cs`

**2. Parameter name used end-to-end:** `IsNppRequest` (bool), plumbed unchanged through `PractitionerSearchPage._isNppEntry` → `PractitionerSearchTable.IsNppRequest` → `ConfirmationWizardModal.IsNppRequest` → `AddNewPractitionerForm.IsNppRequest` / `VerifyExistingPractitionerForm.IsNppRequest`.

**3. Quotes — NPP branches**

`NewPractitionerModel.ShouldRequireField`:
```csharp
public static ValidationResult ShouldRequireField(string value, ValidationContext validationContext)
{
    var model = validationContext.ObjectInstance as NewPractitionerModel;

    if (model.IsNppRequest)
        return ValidationResult.Success;

    if (value == null && !model.IsRecruitmentRole)
    {
        return new ValidationResult("Required field.", new[] { validationContext.MemberName });
    }

    return ValidationResult.Success;
}
```

`NewPractitionerModel.ShouldRequireNpi` (relevant line only):
```csharp
if (string.IsNullOrWhiteSpace(value))
{
    if (model.IsPsgRole || model.IsNppRequest)
    {
        return new ValidationResult("Required field", new[] { validationContext.MemberName });
    }
}
```

`VerifyPractitionerModel.ShouldRequireField`:
```csharp
public static ValidationResult ShouldRequireField(string value, ValidationContext validationContext)
{
    var model = validationContext.ObjectInstance as VerifyPractitionerModel;

    if (string.IsNullOrEmpty(value) && !model.IsRecruitmentRole && !model.IsNppRequest)
    {
        return new ValidationResult("Please enter a valid email address", new[] { validationContext.MemberName });
    }

    return ValidationResult.Success;
}
```

**4. How Verify chooses PropertyDescription vs InputHca**
```razor
@if (IsNppRequest && !string.IsNullOrWhiteSpace(Practitioner?.EmailAddress))
{
    <PropertyDescription Label="Email" Value="@Practitioner.EmailAddress"></PropertyDescription>
}
else
{
    <Form ...><InputHca Id="verifyEmail" @bind-Value="@context.Email" .../></Form>
}
```
NPP + existing (non-blank) email → read-only `PropertyDescription` (same component already used for Gender/DOB/SSN/NPI/Cell/Category on this same card). NPP + blank email, or non-NPP at all → unchanged editable `InputHca` path. `_isFieldRequired` (drives the visual `*`) is also set `false` when `IsNppRequest`, so a blank-email NPP field shows no required asterisk. `ExtendedEmailAddress` is untouched — format is still validated whenever a value is present, regardless of path. Also extended the existing recruitment-only "skip duplicate check when email is blank" guard to also cover NPP (`if (!_model.IsRecruitmentRole && !_model.IsNppRequest)`), so a blank NPP email doesn't trigger an unnecessary/invalid duplicate-check call against `PractitionerDuplicateCheckHelper` — the helper itself was not modified.

**5. Confirm `ShowNoNpiOption` is false for NPP**
Confirmed — `ConfirmationWizardModal.razor` still passes `ShowNoNpiOption="IsNpiOptional"` (unchanged), and `IsNpiOptional` is still sourced only from `PractitionerSearchTable._isNoNpiUser = IsRecruitmentUser || IsPsgUser` (unchanged). `IsNppRequest` was added as a **separate, additional** parameter and never touches `IsNpiOptional`/`ShowNoNpiOption`. An NPP (MSP) user therefore never sees the No-NPI radio/Reason UI.

**6. Build result**
`dotnet build` on `Hca.Credentialing.Client` — succeeded, no warnings or errors.

**7. Remaining gaps**
- No automated test exists (or was created) to lock this behavior — consistent with your instruction not to create a test project.
- `Category` lookup remains the hardcoded APP/DHP/Physician/Other list — untouched, per instructions.
- 131785 (No-NPI/exception UI) and downstream CACTUS/task-card/CVI/CPC behavior remain entirely out of scope and untouched.

**8. Assumptions**
- "Existing email" for the read-only branch is defined as `!string.IsNullOrWhiteSpace(Practitioner.EmailAddress)`, matching how the rest of this form already treats a populated vs. blank email.
- No `[Authorize]`, `PageUri`, `PractitionerSearchForm`, or 136883 redirect logic needed touching — `_isNppEntry` was already computed and simply read one level further down via a new parameter, consistent with the "smallest existing signal" identified in the prior inspection.
