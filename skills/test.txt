You are implementing a change in the HCA HCP Credentialing repository.

ROLE:
Senior engineer. One session. Close 131794 leftovers only. 132247 already UAT-passed — do not edit Verify. Do not start a second chat.

TASK:
1) NPP Add New: remove required * on Email, Cell, DOB, SSN, Gender when IsNppRequest (same as Verify _isFieldRequired). First, Last, Degree, Category, NPI stay required.
2) NPP Add New Verify & Start PAF: empty Email/SSN must not 400 /api/Delegate/Search or leave Validating... forever. Show inline error or skip Delegate search; never navigate to ErrorPage for this 400.

UAT FACTS (treat as true):
- IsNppRequest already plumbed Page → Table → Modal → Add New / Verify. Do not re-plumb.
- NewPractitionerModel.ShouldRequireField already returns Success if IsNppRequest. ShouldRequireNpi already required if IsNppRequest. Do not redo those unless you prove they were reverted.
- Add New stars still show because AddNewPractitionerForm.razor labels/Required are not bound to IsNppRequest. VerifyExistingPractitionerForm already sets _isFieldRequired false when IsNppRequest — copy that pattern onto Add New only.
- SaveNewPractitionerAsync → PractitionerDuplicateCheckHelper → SearchExistingDelegatesAsync. Console: 400 Delegate/Search, then ErrorProcessor → ErrorPage. Helper was not changed in the last shot.
- Modal already skips some duplicate-check-when-email-blank for Verify NPP. Reuse that idea on Add New save if it exists. Do not invent a new helper API unless the current method cannot skip Delegates when email and SSN are both blank.

COMMENTS:
No ticket-ID comments, essays, or region banners. No // 131794. Match existing style. Almost no new comments.

STOP AND REPORT (do not invent):
- If IsNppRequest is gone on Add New.
- If you must edit PractitionerSearchForm, PageUri, Paf.Api, Verify*.razor, ExtendedEmailAddress, Npi.cs, or 136883 redirect.
- If fixing the 400 requires a new CACTUS contract, new auth policy, or NPI N/A UI.

THEN IMPLEMENT (smallest reuse):

A. Stars — AddNewPractitionerForm.razor (+ .cs only if a required bool is missing)
- When IsNppRequest, Email / Cell / DOB / SSN / Gender have no * (Required false / _isFieldRequired false — same name the Add New form already uses, or add one bool like Verify).
- First, Last, Degree, Category stay *. NPI * when IsNppRequest (already ShouldRequireNpi).
- Begin PAF (IsNppRequest false): stars unchanged (those five still *).
- Do not add/remove fields. Do not add No-NPI radio.

B. Delegate 400 / Validating — ConfirmationWizardModal.SaveNewPractitionerAsync (and only the helper if the Delegate call lives only there)
- If Email and SSN are both blank, do not call SearchExistingDelegatesAsync. Practitioner/NPI duplicate check may still run.
- If Delegate search returns 400 or throws HttpRequestException, catch it in this save path: clear the Validating flag, show existing toast/inline error, stay on the modal. Do not let it become ErrorPage.
- Use try/finally (or the existing pattern) so Validating always clears on success, duplicate-found, or failure.
- Begin PAF Add New (email/SSN filled) must still run the full duplicate path as today.
- Prefer not to rewrite PractitionerDuplicateCheckHelper. If you must, only skip Delegates when email and SSN are blank. Do not change FindDuplicatesForRecruitment behavior.

DO NOT CHANGE:
- VerifyExistingPractitionerForm.razor / .razor.cs (132247 closed)
- PractitionerSearchForm, PageUri, 136883 authorize/redirect
- ExtendedEmailAddress.cs, Npi.cs
- Paf.Api, PafTaskSelection, TaskDeterminationService
- ShowNoNpiOption / Recruitment PSG No-NPI
- Add New field list / V5 "starts with 1 or 20"

IMPLEMENTATION REQUIREMENTS:
1. Reuse IsNppRequest and Verify's required-star pattern.
2. Begin PAF unchanged.
3. No new flags, policies, or NPI N/A.
4. Almost no new comments.

TESTS:
- Do not create a test project.
- dotnet build Hca.Credentialing.Client (or the project that hosts these pages).

RETURN:
1. Files changed
2. How Add New * is bound when IsNppRequest (quote)
3. How Delegate search is skipped or 400 handled (quote)
4. Confirm Validating is cleared on failure
5. Confirm Verify files were not edited
6. Confirm ShowNoNpiOption still false for NPP
7. Build result
8. Remaining gaps
9. Assumptions
