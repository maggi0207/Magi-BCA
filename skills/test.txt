Revert the BaseApiClient.cs change from the last shot only.

- Remove HttpStatusCode.BadRequest from the exemption list. Restore prior ErrorProcessor behavior for 400.
- Do not change AddNewPractitionerForm, ConfirmationWizardModal catch, or PractitionerDuplicateCheckHelper skip.
- If a 400 can still fire when email OR ssn is present, intercept only on DelegateClient / SearchExistingDelegatesAsync — not BaseApiClient.
- Optional: Add New NPI Required="@context.IsPsgRole || IsNppRequest" in the non-ShowNoNpiOption branch only.
- Do not edit Verify files.
- Almost no comments.
- dotnet build. RETURN: files changed, quote exemption list after revert, build result.
