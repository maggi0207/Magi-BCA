You are inspecting (NOT implementing) the HCA HCP Credentialing repository.

ROLE:
Senior engineer. READ ONLY. Do not edit files, create files, or run formatters. Do not start implementation. Do not ask for a follow-up inspect chat.

TASK:
Map the PAF Tasks page: how task cards are listed, required/optional badges, Work navigation, Review & Submit enablement, and Delegate / Facility Specific Questions visibility. Answer A–N with file/class/method quotes. Then STOP.

KNOWN STARTING POINTS (confirm or correct — do not glob the whole repo):
- Client/Pages/Paf/Components/PafFormComponent.razor + .razor.cs
- Client/Pages/Paf/Components/PafTaskDisplay.razor + .razor.cs (task card chrome)
- Client/Pages/Paf/Components/ReviewForm/PafReviewForm.razor + .razor.cs — Review & Submit / IsSubmittable()
- Shared/Data/Model/Paf/TaskType.cs — TaskType.Npp [Description] already exists
- Shared/Data/Model/Paf/Form/ — PractitionerDemographicTask, AddressesTask, SpecialtyTask, ManageDelegateTask, Mss18FacilitySpecificQuestionsTask, AddPractitionerToFacilitiesTask
- Hca.Credentialing.Paf.Api CreateAddTaskStrategy(Npp) already maps to AddTaskAddPractitionerToFacilityStrategy (132258)
- PafExtensions.GetCredentialingTypeRtk() has NO Npp case — do not invent an RTK
- MSP check already used for 132256: IsInRole(nameof(HcoApplicationRole.Msp)) on the server. Do not invent a new auth policy.
- If a path is wrong, search only under Client/Pages/Paf/ then Paf.Api TaskDetermination / PafProcessingStrategy. Quote real files.

TICKETS (context only — do not implement):
132260 host — ADD NPP to Facility task card on PAF Tasks after ADD NPP PAF Action. MSP only. Always Required. Align existing card design.
132261 — Title exactly: ADD NPP to Facility. Description exactly: Upload and complete required Non-Privileged Practitioner (NPP) verification information and supporting PSV documentation. Required indicator + Work button.
132262 — Hide card if user cannot access ADD NPP PAF Action. Reuse existing MSP / action access. No new policy.
132263 — Work opens ADD NPP workflow; keep context; on routing error stay on PAF Tasks; no data loss.
132264 — Return to PAF Tasks: complete vs incomplete status from existing task-status pattern.
132265 — ADD NPP card Required for Net New AND Existing.
132266 — Net New Required: Demographic, Manage Addresses, Specialties, ADD NPP to Facility. Existing Optional: Demographic, Manage Addresses, Specialties. Existing Required: ADD NPP to Facility.
132267 — Review & Submit unavailable until required tasks complete; available when they are. If this requires GetCredentialingTypeRtk Npp or a new RTK, STOP and report only.
132268 — ADD NPP PAF Type: do not display Delegate card or Facility Specific Questions card. Other PAF types unchanged.

STOP AND REPORT (do not invent):
- If ADD NPP card cannot be isolated without a new TaskType / auth policy / feature flag (TaskType.Npp already exists — confirm Description text).
- If Review & Submit enablement cannot reuse an existing required-complete gate and would need GetCredentialingTypeRtk Npp or a new RTK (STOP for 132267 only).
- If Delegate/FSQ hide has no existing PAF-type / TaskType branch and you would invent a new suppression framework.
- If Net New vs Existing requiredness has no existing signal (IsNew / Mode) already used for 132255.

ANSWER (quote path + symbol + relevant lines):

A. PAF Tasks page / task-card list: files, how cards are sourced after Save Facilities (132255 add-path). Is there already a TaskType.Npp card? Quote Description on TaskType.Npp.

B. Existing card chrome (PafTaskDisplay or current): title, description, Required badge, Work button. Reuse target for 132261.

C. How Work navigates today (which TaskType → which Tasks/* form). What would Npp Work open today (if anything)? 132263 gap.

D. How required vs optional is decided today (Net New vs Existing). Quote IsNew / TaskAllowedListMode / Required flags. Gap vs 132266.

E. Task status complete/incomplete: where stored, how card refreshes on return. 132264 reuse or gap.

F. Review & Submit: IsSubmittable() or other. Is it gated on required-task completion today? Quote. 132267: existing gate vs STOP (GetCredentialingTypeRtk / new RTK).

G. Delegate card: component + visibility rule. Can ADD NPP / TaskType.Npp hide it without a new framework?

H. Facility Specific Questions / MSS-18 FSQ card: same. Live UAT after ADD NPP save still showed FSQ Required — quote why.

I. MSP / ADD NPP action access on the Tasks page (not the radio). Reuse 132256 IsInRole(Msp)? 132262 gap.

J. Tests locking PAF Tasks cards, requiredness, Review & Submit, Delegate, FSQ. Project + names. If none, say none.

K. Isolation: Begin PAF / Add Practitioner to Facility must not change. Smallest NPP-only branch (TaskType.Npp / PAF type signal).

L. Smallest reuse plan (no code) for 132261–132268. Files that would change. Files that must not change.

M. Confirm: do not open ManageSpecialties/ManageAddresses/VA-PA card internals in this inspect except as Work targets.

N. Remaining blockers for implement (yes/no per child 132261–132268).

DO NOT:
- Implement or edit.
- Open Practitioner Search, 131785, 132352 VA/PA/license/PSV internals, CVI, 132600, CPC.
- Invent AuditAction, auth policy, feature flag, or GetCredentialingTypeRtk Npp case.
- Change CreateAddTaskStrategy / 132255 radio.

RETURN (use these headings only):
1. Files/classes confirmed
2. TaskType.Npp Description + is a Tasks card already rendered?
3. A–N answers (short quotes)
4. STOP conditions hit? (yes/no + which)
5. Ready to implement 132261/132265 card chrome? (yes/no + blocker)
6. Ready to implement 132266 requiredness? (yes/no + blocker)
7. Ready to implement 132267 Review & Submit gate? (yes/no + blocker)
8. Ready to implement 132268 hide Delegate/FSQ? (yes/no + blocker)
9. Ready to implement 132262/132263/132264? (yes/no + blocker)
10. Must-not-change file list
11. Assumptions
