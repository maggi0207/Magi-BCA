You are inspecting (NOT implementing) the HCA HCP Credentialing repository.

ROLE:
Senior engineer. READ ONLY. Do not edit files, create files, or run formatters. Do not start implementation. Do not ask for a follow-up inspect chat.

TASK:
Map existing PAF task-card Work targets that ADD NPP (TaskType.Npp) could reuse: VA/PA questions, state license capture, license recall, PSV upload, duplicate license, routing. Answer A–N with file/class/method quotes. Then STOP.

KNOWN STARTING POINTS (confirm or correct — do not glob the whole repo):
- PafFormComponent.HandleRequestEditAsync — NO TaskType.Npp branch (132260 inspect). Quote the switch and default.
- Client/Pages/Paf/Components/Tasks/AddPractitionerToFacilities/ (or current) — facility / FSQ form opened by AddPractitionerToFacility
- Client/Pages/Paf/Components/Tasks/ — other Work targets
- Shared/Data/Model/Paf/Form/AddPractitionerToFacilitiesTask and any license/PSV task models
- Shared/Clients/Cactus/PractitionerLicenseClient or LicenseClient
- CreateAddTaskStrategy(Npp) => AddTaskAddPractitionerToFacilityStrategy
- CreateUpdateTaskStrategy — no Npp; default throws
- PafExtensions.GetCredentialingTypeRtk() — no Npp; do NOT invent an RTK
- STATUS_RTK / PROVIDERLICENSES_LICENSUREFIELD_RTK — report only if already in code. Do not invent values.
- If a path is wrong, search only under Client/Pages/Paf/Components/Tasks/ then Shared license/image clients and Paf.Api update strategies. Quote real files.

TICKETS (context only — do not implement):
132354 — Card title ADD NPP to Facility; disclaimer; Practitioner Type = NPP only, preselected, required.
132355 — VA question: Is the practitioner an active duty military member or practicing at the VA? Yes/No.
132372 — If VA=YES: Is the Practitioner a PA? Required. Word also says PA=YES → auto-accept + CVI + CPC — REPORT only; do not pick a side.
132365 — VA=YES PA=YES: hide license; Sanctions PSV + NPI PSV; CPC.
132366 / 132367 — license + License/Sanctions/NPI PSV; Effective Date >= today.
132368 / 132369 — recall existing licenses; Add New option; inactive/expired → CPC message.
132374 — duplicate State + License Number block.
132527 — State, Effective Date, License Number max 20, Status STATUS_RTK only, Expiration, Field of Licensure. Named STATUS_RTK: Active DRFB0IQ54S, Temporary Permit D2H11CCT3V, Active Military D3UK1E0UF6, Active-Compact M5890K2GGY.
132529 — entity states CA LA NV KY TX; missing state license blocks submit.
132530 — PSV DOC/DOCX/PDF/JPG/TIFF; Cactus image attrs if already in code.
132531 — routing table (PA=YES / inactive / expired → CPC). Graph 132351 is not this ID.

STOP AND REPORT (do not invent):
- If NPP Work cannot reuse an existing Tasks/* form without a new page AND no existing license/PSV/VA pattern exists — say so; do not invent a card framework.
- If STATUS_RTK / FOL RTKs in the ticket are not in code — list ticket values as gap; do not add them.
- If auto-accept/CVI for PA=YES has no existing strategy — STOP for 132372 extra path only (same as 132591).
- If routing to CPC requires a new status/RTK — STOP for 132531; do not invent.

ANSWER (quote path + symbol + relevant lines):

A. HandleRequestEditAsync switch. What does AddPractitionerToFacility Work open? What would Npp do today (nothing)?

B. Existing facility / FSQ form files. Is there any NPP / VA / PA UI already?

C. Existing state-license capture on any PAF task (fields, STATUS lookup, FOL). Compare to 132527. Quote RTKs already in code vs missing.

D. Existing practitioner license recall / select / add-new. Duplicate State+Number check?

E. Existing PSV / image upload (types, Cactus ParentFile / Type / Publish On Web). Reuse for License/Sanctions/NPI PSV?

F. Existing VA or PA questions anywhere in PAF?

G. CreateUpdateTaskStrategy default throw — confirm. Any Npp update path?

H. Routing to CPC today (PafStatus / strategy). Can 132531 reuse it without a new RTK?

I. Auto-accept / CVI creation — existing entry points? Do not propose implementing 132372 extra path.

J. Tests for license task, PSV upload, VA/PA, CPC route. If none, say none.

K. Isolation: Begin PAF Add Practitioner to Facility / FSQ must not change. Smallest Npp-only Work target (new Tasks/Npp folder vs extend existing form behind TaskType.Npp).

L. Smallest reuse plan (no code) for 132354–132531. Files that would change. Files that must not change.

M. Confirm you did not implement and did not invent RTKs.

N. Ready per child? 132354 chrome, 132355/372 VA-PA, 132368–374 license select, 132527 fields, 132530 upload, 132531 route, 132365–367 paths. Yes/no + blocker each.

DO NOT:
- Implement or edit.
- Open 132260 hide/requiredness implement, 131785, 132600 sync internals, new GetCredentialingTypeRtk case.
- Invent AuditAction, auth policy, STATUS_RTK, FOL RTK, or CVI.

RETURN (use these headings only):
1. Files/classes confirmed
2. Npp Work target today (none / which file)
3. A–N answers (short quotes)
4. STOP conditions hit? (yes/no + which)
5. Ready to implement 132354 card body? (yes/no + blocker)
6. Ready to implement VA/PA (132355/372)? (yes/no + blocker)
7. Ready to implement license + PSV (132527/530/368–374)? (yes/no + blocker)
8. Ready to implement routing 132531? (yes/no + blocker)
9. Reuse vs new Tasks/Npp form (one sentence)
10. Must-not-change file list
11. Assumptions
