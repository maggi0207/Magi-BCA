You are inspecting (NOT implementing) the HCA HCP Credentialing repository.

ROLE:
Senior engineer. READ ONLY. Do not edit files, create files, or run formatters. Do not start implementation. Do not ask for a follow-up inspect chat.

TASK:
Map the existing Add New Practitioner card and Verify Practitioner Information card, and how Begin PAF vs Begin NPP PAF (/Practitioner/Search vs /Practitioner/Search/Npp) reach them. Answer A–L with file/class/method quotes. Then STOP.

KNOWN STARTING POINTS (confirm or correct — do not glob the whole repo):
- NPP search page: Client Practitioner Search with @page /Practitioner/Search/Npp and ForceNpiSearchMode (136878). Begin PAF uses /Practitioner/Search.
- Add New: Client/Pages/PractitionerSearch/Components/ConfirmationWizard/AddNewPractitionerForm.razor + .razor.cs; ConfirmationWizardModal; NewPractitionerModel (or current names).
- Results / Start / Add New button: PractitionerSearchTable (or current).
- If a path is wrong, search only under Client/Pages/PractitionerSearch/ then quote the real files.

TICKETS (context only — do not implement):
131794/131795 — On Begin NPP PAF Add New only: Required First Name, Last Name, Degree, Provider Category, Individual NPI. Optional Email, Cell, DOB, SSN, Gender, Suffix. Character limits / Category lookup / NPI validate / duplicate NPI = reuse existing. Begin PAF Add New must stay unchanged.
132247/132250/132252/132248/132253 — On Begin NPP PAF Verify existing only: if email exists → show read-only, no edit. If blank → editable optional, Start PAF allowed. Validate format only when a value is entered. Begin PAF Verify and Add New (131794) unchanged.

STOP AND REPORT (do not invent):
- If there is no existing flag/route/mode to isolate NPP vs Begin PAF on these forms (do not invent a feature flag or auth policy).
- If completing 131795 requires NPI N/A / exception UI (that is 131785 — report only).
- If Add New and Verify share requiredness with no branch for "NPP existing email" vs "NPP new required set".

ANSWER (quote path + symbol + relevant lines):

A. Add New component files. Required vs optional today for: First Name, Last Name, DOB, Gender, SSN, Email, Cell, Degree, Provider Category, Individual NPI, Suffix, Middle Name.

B. How requiredness is applied (DataAnnotations, FluentValidation, AntDesign Form, custom). Quote the attributes/rules.

C. How the form knows Begin PAF vs NPP (route, ForceNpiSearchMode, SearchMode, cascade, parameter). Quote the branch. If there is NO branch yet, say that and name the smallest existing signal the form can read (e.g. parent ForceNpiSearchMode / URI).

D. Provider Category lookup: where values come from (do not invent an RTK name if not in code).

E. Individual NPI validation + duplicate NPI check: method names. Any existing NPI N/A / exception on this form? (Expect none.)

F. Verify Practitioner Information component files (opened by Start on an existing row). Quote title "Verify Practitioner Information" / "Update Email Address" if present.

G. How existing email is loaded onto Verify. Is the Email field required today (Email*)? Is it bound read-only anywhere?

H. Email format validator: class/method. Does blank trigger a required error today?

I. Existing read-only field pattern on this wizard or nearby forms (reuse target for 132250). Quote one.

J. Tests that lock Begin PAF Add New or Verify requiredness. List project + test names. If none, say none.

K. Smallest safe extension point for 131794/131795 (NPP-only optional demographics + NPI still required). Files that would change. Files that must not change.

L. Smallest safe extension point for 132247 (NPP-only existing email rules). Files that would change. Files that must not change.

DO NOT:
- Implement or edit.
- Open PafTaskSelection, TaskDeterminationService, CACTUS CRUD, CPC, CVI.
- Change or propose NPI search / No-NPI exception.
- Invent authorization policies, AuditAction, feature flags, or V5 "NPI starts with 1 or 20" unless already in code.
- Browse outside PractitionerSearch + tests that reference these types.

RETURN (use these headings only):
1. Files/classes confirmed (Add New + Verify)
2. Begin PAF vs NPP isolation signal (quote)
3. A–L answers (short quotes)
4. STOP conditions hit? (yes/no + which)
5. Ready to implement 131794+131795? (yes/no + blocker)
6. Ready to implement 132247 family? (yes/no + blocker)
7. Must-not-change file list
8. Assumptions
