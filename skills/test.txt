You are implementing a change in the HCA HCP Credentialing repository.

ROLE:
Act as a senior engineer familiar with the existing repository patterns.

TASK:
Implement feature 132255 in this single session: Practitioner Action radio "ADD NPP to Facility" for eligible MSP users, single-select preserved, submit mapped onto existing New/Existing PAF task workflows. Inspect the remaining gaps first in this same turn, then code. Do not stop after inspect unless a STOP condition below is hit.

CHILDREN IN THIS SHOT:
132256 Display radio for MSP — last in list, exact label, hide non-MSP, fail-closed + log if role cannot be retrieved.
132257 Restrict to eligible practitioners — SHOW net new OR existing inactive with entity; HIDE active / other ineligible.
132259 Enforce single PAF action selection — keep radio single-select including the new option; client + server must not accept multiple actions.
132258 Apply existing PAF task determination — on submit of ADD NPP, reuse New Practitioner vs Existing Practitioner rules; no separate NPP determination engine; on failure show error, block submit, log, no partial task set.

BUSINESS REQUIREMENT:
Enable MSP users to add a Net New Practitioner or an existing practitioner inactive with the selected entity via a dedicated PAF Action while reusing existing task determination.

Exact radio label: ADD NPP to Facility

REPOSITORY EVIDENCE (already inspected — treat as fact unless you re-read and disprove):
- Dialog: Client/Pages/Paf/Components/PafTaskSelection.razor + .razor.cs. GetTitle() returns "PAF Action & Facilities" for non-PSG/non-Recruitment (~line 154). Caller: PafFormComponent, ShowFacilities.
- Radios: AntDesign RadioGroup @bind-Value _selectedTask TValue=TaskType; foreach _currentAvailableTasks. Already structurally single-select.
- List: LoadAvailableTasks → PafClient.GetAvailableTaskListAsync(TaskListParameters { Mode, PractitionerId, IsPsgUserMode, IsRecruitmentUserMode }) → TaskController → TaskDeterminationService.GetAvailableTaskListAsync.
- Existing-practitioner last option: "Add Privilege MSS-18". New-practitioner list today: only "Add Practitioner to Facility" (re-confirm by reading the method).
- TaskType.Npp exists: Shared/Data/Model/Paf/TaskType.cs [Description("NPP PAF")] — NOT in the list. Unwired. No AddTaskNppStrategy.
- Save: OnSaveClicked → TaskSelectionEvent.PafType (TaskType?) → HandleTaskSelectionEvent → AddTaskToPafAsync([FromQuery] TaskType task) → CreateAddTaskStrategy(task). Npp will fail without a mapping.
- This dialog does not check MSP. Only IsPsgUser()/IsRecruitmentUser(). Fallthrough is NOT MSP-only.
- 136883 IsMspOnlyUser() is private on PafQueueSearch.razor.cs: IsInRole(nameof(HcoApplicationRole.Msp)). Do not move that method. Search for existing IsMspUser() extension; else use IsInRole(Msp) idiom already used for PSG/Recruitment in this dialog.
- Facility active/inactive is applied in GetAvailableFacilityListAsync via GetUserFacilitiesNotAddedToOrInactiveAtAsync for AddPractitionerToFacility, NOT at radio visibility. PractitionerSummary.Active semantics are unconfirmed — do not use it unless you open the CACTUS mapping and prove it is per-entity.
- Tests: none for this dialog / TaskDeterminationService. Do not break CrudEntityCredentialingOperationTests GetMsoDueDate_ReturnsNull_ForAddPractitionerToFacilityWithTemporaryCredentialingTypeRtk.

STOP AND REPORT (do not invent):
- If mapping Npp onto existing add-task requires a new CACTUS RTK constant that is not already in code.
- If GetCredentialingTypeRtk (or equivalent) is invoked on this AddTask path and cannot reuse the AddPractitionerToFacility case without a new RTK.
- If inactive-at-entity cannot be evaluated with an existing query (GetUserFacilitiesNotAddedToOrInactiveAtAsync or proven per-entity field). Do not guess PractitionerSummary.Active.

BEFORE CODING (same turn — quote paths/methods):
1. TaskDeterminationService.GetAvailableTaskListAsync — New and Existing non-recruitment branches; last item each.
2. GetAvailableFacilityListAsync — AddPractitionerToFacility inactive/not-added filter; how userId is obtained.
3. TaskController GetAvailableTaskList / AddTaskToPaf — client role flags vs HttpContext.User. Prefer server ClaimsPrincipal for MSP.
4. PafProcessingStrategyContext.CreateAddTaskStrategy — switch/default for unknown / Npp. Quote it.
5. AddTaskAddPractitionerToFacilityStrategy — what it does on add (reuse target for 132258).
6. Whether GetAvailableTaskListAsync can call the same facility query as GetAvailableFacilityListAsync (userId + practitionerKey).
7. PafTaskSelection Save/Next disable pattern.
8. Any existing IsMspUser() extension.

THEN IMPLEMENT (smallest reuse):

132256
- Add TaskType.Npp as LAST TaskDto in MSP-applicable NewPractitioner and ExistingPractitioner branches (not PSG, not Recruitment).
- TaskDto.Name MUST be exactly "ADD NPP to Facility". Do NOT use GetDescription() ("NPP PAF"). Do not change [Description] on the enum in this shot unless it is unused everywhere; prefer Name override.
- Explicit MSP: IsInRole(nameof(HcoApplicationRole.Msp)) or existing IsMspUser(). Not "not PSG and not Recruitment".
- If role cannot be retrieved (exception on AuthenticationState / null principal when MSP cannot be evaluated): do not add Npp; log warning/error with existing ILogger. Other actions unchanged.
- Prefer server-side MSP check if TaskController/service has the user. If PSG/Recruitment flags are client-supplied, still do not trust a client-only "isMsp" without also checking the API user when possible.

132257
- SHOW Npp when Mode is NewPractitioner (net new) AND MSP.
- SHOW Npp for ExistingPractitioner only if existing facility-eligibility query indicates the practitioner can be added or is inactive at a user facility (reuse GetUserFacilitiesNotAddedToOrInactiveAtAsync or the same repository method). If that list is empty → HIDE (treat as active/ineligible).
- Do not hide other actions with this rule.
- Do not wait for a facility checkbox to show/hide the radio (AC is on Practitioner Action load). Using "has at least one eligible facility" is the allowed interpretation of "inactive with the entity" at load time because no facility is selected yet. Document that interpretation in RETURN.
- GetAvailableFacilityListAsync for TaskType.Npp must use the SAME facility filter as AddPractitionerToFacility so the right-hand picker matches eligibility.

132259
- Keep the existing RadioGroup; do not switch to checkboxes.
- Do not change AddTaskToPafAsync signature (already a single TaskType).
- If you find a realistic multi-select path, reject + log with existing ILogger. If structurally impossible, do not invent a dummy validator; note that in RETURN.

132258
- Do NOT create a new determination service or a parallel GetAvailableTaskList engine.
- On AddTaskToPafAsync(TaskType.Npp), map onto the existing AddPractitionerToFacility add-task workflow: prefer CreateAddTaskStrategy(Npp) delegating to AddTaskAddPractitionerToFacilityStrategy (or the factory calling that same strategy) so New vs Existing Mode already on the PAF continues to apply.
- A new AddTaskNppStrategy class is allowed ONLY as a thin wrapper that reuses that existing strategy — that is the existing per-TaskType factory pattern, not a new engine.
- If add-task fails: existing error UI, do not save a partial PAF, log. Follow how AddTaskToPafAsync already handles strategy exceptions; extend only if Npp would otherwise save half-state.
- Do not implement NPP-specific task cards, VA/PA, Delegate/FSQ suppression (132275).
- If GetCredentialingTypeRtk (or switch on TaskType during this add path) would throw for Npp, add Npp to the SAME case as AddPractitionerToFacility. Do not add new RTK string literals.

DO NOT CHANGE:
- RadioGroup/foreach structure in PafTaskSelection.razor (disable-Save binding OK).
- AddTaskToPafAsync / TaskSelectionEvent / TaskDto shapes except TaskListParameters if that is how role flags already work.
- Other TaskType list entries' order and rules.
- PafQueueSearch.razor.cs.
- Begin PAF / NPI search.
- Do not invent authorization policies, AuditAction, or CACTUS RTKs.
- Do not add a New badge.

IMPLEMENTATION REQUIREMENTS:
1. Reuse existing patterns.
2. Preserve other PAF actions.
3. No duplicate dialog.
4. Existing DI, logging, validation.
5. Do not invent business rules.
6. Unrelated code unchanged.

TESTS:
- If Paf.Api (or matching) test project exists, add the smallest tests in existing style: MSP+net-new sees Npp last named "ADD NPP to Facility"; non-MSP/PSG/Recruitment do not; existing-active-with-no-eligible-facility hides Npp; other actions unchanged; AddTask Npp reuses add-facility path and does not throw in the factory.
- If no such test project, say so; do not create a new test project.
- Build. Do not break the AddPractitionerToFacility CVI due-date test named above.

VALIDATION: build, relevant tests, inspect diff.

RETURN:
1. Files changed
2. Classes/methods changed
3. Quotes from CreateAddTaskStrategy and how Npp save is mapped
4. How MSP is evaluated (client/server)
5. Exact TaskDto.Name
6. Eligibility query used for 132257 and the load-time interpretation
7. Facility list handling for Npp
8. 132259: any extra server check or "already scalar"
9. Tests / build results
10. Remaining gaps (especially 132275)
11. Assumptions
