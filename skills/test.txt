136871 remaining work only. 136873 is DONE. Do not redo or revert the Begin NPP PAF button.

ROLE: senior HCP Blazor engineer. Complete remaining 136872 children. Do not scan the whole repo.

IF STUCK, read only these analysis files (skip if not in this workspace):
- 08_Office_Repository_Evidence / ADD NPP code-level architecture (search + Begin PAF)
- 04_GAP_ANALYSIS existing-vs-required (entry-point rows; do not treat gap as automatically correct)
- ADD_NPP_MASTER_IMPLEMENTATION_PLAN.md §9 (136873 was blocked on shared visibility; that is resolved)
- 136873_evidence.md
Treat source code as truth over those docs. Do not rediscover architecture already listed below.

ALREADY ESTABLISHED (do not re-investigate):
- Begin NPP PAF is in PafQueueSearchCriteria.razor Extra template, non-Recruitment else branch, beside Begin PAF.
- CanBeginPaf comes from PafQueueSearch.CanBeginPAF(): true if IsMspUser() (Msp OR Psg) OR IsRecruitmentUser() (Recruiter OR RecruiterManager).
- Begin PAF / Begin Request both call NavigateToStartPaf() → PageUri.PractitionerSearchUri with NO mode/query.
- Do NOT assume that URI is Enforce NPI Search. UI: Begin PAF search is NPI+SSN+name; Enforce NPI Search is NPI-required + No NPI exception. Prove from source.
- PractitionerSearchForm has SearchMode.NpiSearch / ExceptionSearch (architecture evidence; confirm in source before using).
- MspDashboardPage: /Msp/Dashboard and /Psg/Dashboard, [Authorize(Roles="Msp, Psg")].
- SharedPafQueue is also on CPC, CSS, Recruitment, Admin. Do not show NPP on Recruitment (Begin Request branch).
- Tooltip mockup extra sentence is NOT approved.
- 53658 is out of scope (blocked until 136871 deployed).

ORDER:
1. Confirm 136873 markup. Do not change Begin PAF / Begin Request / NavigateToStartPaf for Begin PAF.
2. 136876
3. 136878 (inspect search target first)
4. 136883
5. 136882 regression throughout
6. Client build + git diff. Stop.

---
136876 — FILE: Client/Pages/Queues/Components/PafQueueSearchCriteria.razor
Exact text: Use this option only when submitting a request for a Non-Privileged Practitioner (NPP).
Grep Client only: Tooltip, Title=, info-circle (or similar) with Button. Reuse that pattern. Hover AND focus. NPP control only.

---
136878
Inspect only: PageUri.cs; PractitionerSearchPage; PractitionerSearchForm (SearchMode); how PSG/Recruitment open NPI-required search; PafQueueSearchCriteria.razor.cs NavigateToStartPaf.
Wire Begin NPP PAF click only. No new search. If you cannot distinguish Enforce NPI Search from Begin PAF search, STOP.

---
136883
MSP may use Begin NPP PAF. Hide is not enough; block direct URL/mode. Reuse existing [Authorize]/IsInRole. Do not invent a policy. Do not break Begin PAF or existing PSG/Recruitment search.

---
136882
Begin PAF label, visibility, click, NavigateToStartPaf, Begin Request must stay equivalent.

Build Client. git diff = only files required for 136876/136878/136883.

RETURN:
1. 136871 COMPLETE / PARTIALLY COMPLETE / BLOCKED
2. Each child: status, files, validation
3. NPP flow
4. Security
5. Begin PAF / Begin Request
6. Build
7. Every changed file + why
8. Blockers only if genuine
